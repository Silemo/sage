## Summary
Fix the review-blocking test issues identified after the scope-filter change: remove assertion side effects from the legacy JSON normalization test, repair stale source-precedence assertions in the requirements suite, and restore the downstream follow-up suite to a trustworthy green state. This is a test-only follow-up; no production behavior changes are required.

## Steps

### Step 1: Make the legacy JSON normalization test explicit and side-effect free
- **File**: `tests/name-teams-decoupling-spec.mjs` (modify)
- **Changes**:
  - In the test `legacy JSON day still supports team filtering through fallback team normalization`, replace the current pipeline:
    - `jsonRecords.map(...).filter((result) => { assert.equal(result.errors.length, 0); return result.record; }).map(...)`
  - With a two-pass structure:
    - `const normalizeResults = jsonRecords.map((record) => normalizeRecord(record, "data/json/2026-03-18.json", "2026-03-18"));`
    - `normalizeResults.forEach((result) => assert.equal(result.errors.length, 0));`
    - `const normalized = normalizeResults.filter((result) => result.record).map((result) => result.record);`
  - Keep the existing filter assertions unchanged so the test still proves legacy JSON records normalize into the modern team-filter contract.
- **Rationale**: This removes assertion side effects from `.filter()`, making failures clearer and aligning the test with EMA readability expectations.
- **Tests**:
  - Run `node tests/name-teams-decoupling-spec.mjs`
  - Confirm the legacy JSON test still passes and failure output remains readable if intentionally broken later.
- **Commit message**: `test(json): make legacy normalization assertions explicit`

### Step 2: Repair stale source-precedence assertions in the requirements suite
- **File**: `tests/sage-spec-requirements.mjs` (modify)
- **Changes**:
  - Update `loader accepts committed CSV data and keeps legacy JSON fallback compatible` so it no longer expects `loadAllSources(...)` to return day-two events sourced from `data/json/2026-03-18.json`.
  - Split the assertions inside that test into two explicit concerns while keeping the same test name unless renaming becomes necessary:
    1. **CSV precedence contract**:
       - Keep `const result = await loadAllSources(sources);`
       - Assert there are day-one CSV events from `data/csv/2026-03-17.csv`
       - Assert there are day-two CSV events from `data/csv/2026-03-18.csv`
       - Assert the loaded dates remain `2026-03-17` and `2026-03-18`
    2. **Legacy JSON compatibility contract**:
       - Import `normalizeRecord` from `../js/loader.js` if not already imported
       - Fetch `data/json/2026-03-18.json` directly with the existing `fetchJson(...)` helper
       - Normalize all records with `normalizeRecord(record, "data/json/2026-03-18.json", "2026-03-18")`
       - Assert the normalized JSON results contain zero errors and produce records with string `name` and array `teams`
  - Update `loader skips malformed committed CSV rows without aborting the schedule` so the `VS PLM Plenary` exclusion assertion is scoped to the mutated day-one data only:
    - Replace `result.events.every((event) => event.name !== "VS PLM Plenary")`
    - With `result.events.filter((event) => event.date === "2026-03-17").every((event) => event.name !== "VS PLM Plenary")`
  - Preserve the current test names if possible so downstream follow-up tests do not need to change their expected output strings.
- **Rationale**: The failures are stale assumptions introduced by the repo’s current CSV-preferred source layout, not by the scope-filter feature. Fixing them at the assertion level restores the suite as a reliable regression gate without changing production code.
- **Tests**:
  - Run `node tests/sage-spec-requirements.mjs`
  - Confirm the suite reports `PASS 5/5`
  - Confirm no assertion still relies on day-two events being loaded from JSON through `loadAllSources(...)`
- **Commit message**: `test(requirements): align source assertions with csv precedence`

### Step 3: Re-validate the downstream follow-up suite and add light robustness notes
- **Files**:
  - `tests/sage-review-followups-spec.mjs` (modify only if Step 2 forces a name/output change)
  - `tests/scope-filter-all-events-spec.mjs` (optional modify)
  - `js/filter.js` (optional modify)
- **Changes**:
  - First re-run `tests/sage-review-followups-spec.mjs` unchanged. If Step 2 preserved the existing `PASS 5/5` summary and test names, do not edit this file.
  - Only if the requirements suite output changed, update `tests/sage-review-followups-spec.mjs` so its subprocess expectations match the new stable output while preserving the intent of the test:
    - valid committed data growth should keep the requirements suite green
    - the runner should continue after an early failure and still emit later PASS lines
  - Address the two info-level review notes only if they can be done without widening scope:
    - Add a short comment in `tests/scope-filter-all-events-spec.mjs` above the ordered `deepEqual(...)` expectations noting the arrays are start-time ordered via `sortEventsByStart()`
    - Do **not** change `js/filter.js` in this follow-up unless you decide to take the optional semantics cleanup; if you do, limit it to `buildHierarchy()` by extending the skip guard to `|| isAllValueStreamEvent(event)` and verify no hierarchy tests change behavior
- **Rationale**: The main goal is to get the full suite green again. The downstream follow-up file should only change if the repaired requirements suite output contract changed. The info-level items are optional and should not expand a test-repair task into a production refactor unless the implementer can prove zero behavioral impact.
- **Tests**:
  - Run the full suite:
    - `node tests/sage-spec-requirements.mjs`
    - `node tests/sage-review-followups-spec.mjs`
    - `node tests/requirements.spec.mjs`
    - `node tests/name-teams-decoupling-spec.mjs`
    - `node tests/scope-filter-all-events-spec.mjs`
    - `node tests/date-named-sources-spec.mjs`
  - Expected result: `25/25` passing overall
- **Commit message**: `test(suite): restore full regression suite green`

## Testing Approach
1. Fix the local readability issue first:
   - `node tests/name-teams-decoupling-spec.mjs`
2. Repair the stale requirements assertions next:
   - `node tests/sage-spec-requirements.mjs`
3. Re-run the downstream subprocess-based suite:
   - `node tests/sage-review-followups-spec.mjs`
4. Finish with the full regression surface:
   - `node tests/sage-spec-requirements.mjs; node tests/sage-review-followups-spec.mjs; node tests/requirements.spec.mjs; node tests/name-teams-decoupling-spec.mjs; node tests/scope-filter-all-events-spec.mjs; node tests/date-named-sources-spec.mjs`

## Handoff

**Artifact saved**: `artifacts/2026-03-24-scope-filter-review-followups-plan.md`

**Upstream artifacts**:
- Plan: `artifacts/2026-03-24-scope-filter-all-events-plan.md`
- Implementation: `artifacts/2026-03-24-scope-filter-all-events-implementation.md`
- Test report: `artifacts/2026-03-24-scope-filter-all-events-test-report.md`
- Review report: `artifacts/2026-03-24-scope-filter-all-events-review-report.md`
- Architecture: none
- Requirements: none

**Context for @ema-implementer**:
- 3 steps to execute
- Files to modify:
  - `tests/name-teams-decoupling-spec.mjs`
  - `tests/sage-spec-requirements.mjs`
  - `tests/sage-review-followups-spec.mjs` only if requirements-suite output changed
  - `tests/scope-filter-all-events-spec.mjs` optional comment-only clarification
  - `js/filter.js` optional only if taking the info-level `buildHierarchy` semantics cleanup
- Files to keep unchanged unless Step 3 explicitly requires it:
  - `app.js`
  - `js/loader.js`
  - committed CSV/JSON data files
  - `tests/requirements.spec.mjs`
- Test command: `node tests/sage-spec-requirements.mjs; node tests/sage-review-followups-spec.mjs; node tests/requirements.spec.mjs; node tests/name-teams-decoupling-spec.mjs; node tests/scope-filter-all-events-spec.mjs; node tests/date-named-sources-spec.mjs`
- Test framework: custom Node assertion scripts using `node:assert/strict`
- Watch for:
  - preserve the existing `PASS 5/5` output contract in `tests/sage-spec-requirements.mjs` if possible, to avoid unnecessary churn in `tests/sage-review-followups-spec.mjs`
  - do not “fix” production logic unless you intentionally take the optional `buildHierarchy` semantics cleanup and can verify no behavior change
  - do not reintroduce day-two JSON assumptions into `loadAllSources(...)`; legacy JSON compatibility should be tested through `normalizeRecord(...)`

> 📋 **Model**: Select **GPT-5.4** before invoking `@ema-implementer`

**What @ema-implementer should do**:
1. Read this plan artifact first, then the review report
2. Implement Step 1 exactly in `tests/name-teams-decoupling-spec.mjs`
3. Repair the stale requirements assertions in `tests/sage-spec-requirements.mjs` without changing production code
4. Re-run `tests/sage-review-followups-spec.mjs` and only edit it if the repaired requirements-suite output contract changed
5. Run the full suite and confirm `25/25` pass before handing off
6. Save the implementation summary to `artifacts/2026-03-24-scope-filter-review-followups-implementation.md`
